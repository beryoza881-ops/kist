# 자율실험실(SDL) 도식 – 자율실험 폐루프 예시 이미지 추가

`v4_new_mod` 도식의 **자율실험 폐루프** 안 1~4번 박스에 각 단계를 설명하는
예시 이미지를 추가한 버전입니다. **텍스트 내용은 원본과 100% 동일**하며
(텍스트 런 227개 전부 일치), 이미지 삽입 공간을 확보하기 위한
레이아웃(여백·박스 높이·연결선 위치)만 조정했습니다.

## 세 가지 버전

| 버전 | 파일 | 이미지 |
|---|---|---|
| **A. 아이콘** | `자율실험실_SDL_도식_v5_예시이미지.pptx` | 납작한 벡터 아이콘 (박스당 3분할 스트립 1장) |
| **C. 장면 일러스트** ★ | `자율실험실_SDL_도식_v6_장면일러스트.pptx` | 직접 그린 사실적 장면 12장 (박스당 3장) — **완성** |
| **B. 사진** | `자율실험실_SDL_도식_v5-photo_사진버전.pptx` | 실제 사진 12장 자리 (자리표시자 상태) |

**C가 현재 권장안**입니다. 사진을 구할 수 없는 환경이라 12장을 직접 그렸습니다.
원근·명암·재질 그라디언트를 넣어 실사에 가깝게 그린 벡터 장면이며,
사진 버전과 동일한 3:2 타일 규격이라 나중에 실제 사진으로 한 칸씩 교체할 수 있습니다.

> **B(사진 버전)는 현재 자리표시자 상태입니다.** 이 작업 환경의 네트워크 정책이
> Wikimedia Commons·Unsplash·Pexels·Pixabay 등 모든 이미지 호스트를 차단하고 있어
> 사진을 내려받을 수 없었습니다. `photos/` 에 사진 12장을 넣고
> `python3 assets/apply_photo_layout.py` 를 실행하면 완성됩니다.
>
> **→ 먼저 `사진_넣는_안내표.png` 를 보세요.** 12칸 각각에 대해
> 파일명 / 실제로 무엇이 찍힌 사진이어야 하는지 / 검색어 / 사진 난이도를 정리했습니다.
> PPTX 안의 자리표시자에도 같은 내용이 찍혀 있어, 확대하면 어느 칸에 무엇을
> 넣는지 바로 보입니다.
>
> 12칸 중 `box1_1`(AI 추천엔진) · `box3_2`(목표충족 자동판정) · `box4_2`(파인튜닝 파이프라인)은
> **실물이 없는 개념이라 사진으로 표현이 안 됩니다.** 이 3칸만 일러스트를 쓰는
> 혼합안을 권합니다.

## 산출물

| 파일 | 설명 |
|---|---|
| `자율실험실_SDL_도식_v5_예시이미지.pptx` | 일러스트 버전 (완성) |
| `자율실험실_SDL_도식_v6_장면일러스트.pptx` | 장면 일러스트 버전 (완성, 권장) |
| `자율실험실_SDL_도식_v5-photo_사진버전.pptx` | 사진 버전 (사진 대기) |
| `preview.jpg` / `preview_scene.jpg` / `preview_photo.jpg` | 각 버전 렌더링 미리보기 |
| `사진_넣는_안내표.png` | **12칸 대응표** — 파일명·피사체·검색어·난이도 (사진 버전 필독) |
| `photos/` | 사진 넣는 곳 + 파일명·규격·라이선스 안내 |
| `assets/원본_v4_new_mod.pptx` | 입력 원본 |
| `assets/sdl_layout.py` | 공통 레이아웃 좌표 테이블 (두 빌드 스크립트가 공유) |
| `assets/make_example_images.py` | 일러스트(SVG→PNG) 생성 |
| `assets/apply_layout.py` | 일러스트 버전 빌드 |
| `assets/make_photo_placeholders.py` | 사진 자리표시자 타일 생성 (파일명·검색어 각인) |
| `assets/make_photo_guide.py` | 사진 넣는 안내표 생성 |
| `assets/make_scene_images.py` | **장면 일러스트 12장 생성** |
| `assets/scene_images/` | 장면 일러스트 원본 (SVG + PNG) |
| `assets/apply_photo_layout.py` | 사진 버전 빌드 (크롭·모서리·테두리 포함) |

## 각 박스에 들어가는 예시

박스의 `ex.` 항목을 그대로 시각화했습니다.

| 박스 | 좌 | 중 | 우 |
|---|---|---|---|
| 1. AI 실험 설계 | AI 추천엔진 | 과거 실험 DB | 가상실험 모델 |
| 2. 로봇·장비 실험 수행 | 로봇팔 | 액체 핸들러 | 측정장비 |
| 3. AI·분석SW의 결과분석 | 측정데이터 분석 | 목표충족 자동판정 | 시각화 대시보드 |
| 4. AI 모델 갱신 | 모델 재학습 | 파인튜닝 파이프라인 | 다음조건·종료 결정 |

## 버전 A – 일러스트

전량 **직접 제작한 원본 SVG 벡터 일러스트**입니다. 외부 스톡 이미지·아이콘 폰트를
사용하지 않았으므로 저작권·라이선스 제약이 없습니다. 원본 도식의 팔레트
(`#1D3567` `#21407C` `#1B54B5` `#C7D4EC` `#F4F6FA` `#DDE0E6`)를 그대로 사용해
기존 디자인과 톤을 맞췄습니다. 박스당 이미지 1장(2.625" × 0.62")입니다.

## 버전 C – 장면 일러스트 (권장)

사진을 내려받을 수 없어 12장을 전부 손으로 코딩해 그렸습니다. 납작한 아이콘이 아니라
**원근·명암·재질**이 들어간 장면입니다.

| 박스 | 좌 | 중 | 우 |
|---|---|---|---|
| 1 | 어두운 연구실 모니터(코드+신경망) | 데이터센터 서버랙 | 3D 분자 모형 |
| 2 | 6축 로봇팔(바이알 파지) | 8채널 피펫 + 96웰 플레이트 | 분석기기 전면 패널 |
| 3 | 크로마토그램 모니터 | QC 판정 화면(규격선 + PASS) | 대시보드 화면 |
| 4 | GPU 서버 내부 | 코드 에디터 + 학습 진행바 | 분기 결정 화면 앞 연구자 |

기법: `linearGradient`/`radialGradient`로 금속·유리·구체 재질, 1점 투시(서버랙·웰플레이트),
바닥 그림자와 비네트, 화면 발광(glow). SVG → PNG(2배 렌더 후 축소)로 앤티에일리어싱 확보.
**전량 직접 제작한 원본이라 저작권 제약이 없습니다.**

## 버전 B – 사진

같은 이미지 밴드를 **사진 3장(각 0.8383" × 0.58", 약 3:2)** 으로 나눴습니다.

- 원본 사진 비율과 무관하게 **가운데 기준 자동 크롭**(`srcRect`)됩니다. 세로 사진도 됩니다.
- 각 사진은 독립 도형이라 PowerPoint에서 **[그림 바꾸기]로 개별 교체**할 수 있습니다.
  (스크립트를 다시 돌리지 않아도 됩니다)
- 모서리 라운딩 + 얇은 테두리(`#DDE0E6`)를 적용해 카드 디자인과 통일했습니다.

## 레이아웃 변경 내역 (두 버전 공통)

이미지 밴드(카드당 2.625" × 0.62")를 넣기 위해 세로 공간 약 1.5"를 확보했습니다.

- 1~4번 카드 높이 **1.62" → 2.372"** (텍스트 아래에 이미지 밴드 배치)
- 상단 여백 0.71" → 0.22", `개념`/`정의` 박스 높이 0.85" → 0.64"
- `연구자` 박스, 폐루프 컨테이너 내부 여백, 하단 `자동화 실험실과의 차이` 박스 높이 축소
- 폐루프 컨테이너 높이 5.0" → 6.46", 순환 화살표·점선 연결선·우측 지원 요소
  (`표준 인터페이스·드라이버`, `AI-Ready 데이터`)를 새 행 위치에 맞춰 재정렬
- 1번 카드의 `ex.` 텍스트 상자 폭만 2.33"로 좁아 줄바꿈이 과했던 문제를
  나머지 카드와 동일한 2.625"로 정렬 (원본 불일치 수정)

## 재현 방법

```bash
pip install python-pptx cairosvg Pillow

# 버전 A (일러스트)
python3 assets/make_example_images.py
python3 assets/apply_layout.py

# 버전 C (장면 일러스트)
python3 assets/make_scene_images.py
python3 assets/apply_photo_layout.py --scenes

# 버전 B (사진) — photos/ 에 사진을 넣은 뒤
python3 assets/make_photo_placeholders.py   # 자리표시자 타일
python3 assets/make_photo_guide.py          # 사진 넣는 안내표
python3 assets/apply_photo_layout.py
```
